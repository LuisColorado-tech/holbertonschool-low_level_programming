#include <stdio.h>
/**
 * main - Entry point
 * description: plair
 * LG
 * Return: Always 0 PR
 */
int main(void)
{
	char c;

	for (c = 'z'; c >= 'a'; c--)
		putchar (c);
		putchar ('\n');

	return (0);
}
