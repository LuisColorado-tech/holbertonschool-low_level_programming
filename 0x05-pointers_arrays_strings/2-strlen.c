#include "holberton.h"
#include <stdio.h>
/**
 * _strlen - R the l of a s.
 * @s: Type char pointer
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
	{
	}
		return (c);

}
